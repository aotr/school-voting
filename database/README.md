Portable SQLite database folder.

Recommended production file:

- `database/voting.db`
- `database/schema.sql`

Keep database paths relative to the app root so the whole folder can be copied to another machine without reconfiguration.
